# Next Steps

## Near-Term

- Add dependency review when package manifests are introduced.
- Add CodeQL when source language is selected.
- Add SBOM generation for release artifacts.
- Add artifact integrity checks for packaged outputs.

## Promotion Criteria

This lab can become an accelerator when:

- CI passes consistently.
- Security review confirms workflow permissions.
- Evidence output is stable and useful.
- Documentation includes setup, operation, and limitations.
- A reusable workflow template is extracted under `accelerators/github-actions/`.

## Accelerator Candidate

Target accelerator path:

```text
accelerators/github-actions/security-baseline/
```
